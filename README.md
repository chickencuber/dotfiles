this is all my dotfiles

you have to manually handle limine catppuccin theme, and the kernel args, but everything else should be handled for you

just make sure your inside the repo directory, and run `./hidden/bootstrap-arch.sh`

first clone the repo with `git clone --recurse-submodules https://github.com/chickencuber/dotfiles.git`
